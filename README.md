# Workout Calendar Generator

This Python package generates a monthly workout calendar based on a given month. It creates an `.ics` file that you can import into Google Calendar.

## Installation

```bash
pip install .
